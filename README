Global setup:
 Set up git
  git config --global user.name "Your Name"
  git config --global user.email cssrain@gmail.com
      
Next steps:
  mkdir Sharp-jQuery-Source
  cd Sharp-jQuery-Source
  git init
  touch README
  git add README
  git commit -m 'first commit'
  git remote add origin git@github.com:cssrain/Sharp-jQuery-Source.git
  git push -u origin master
      
Existing Git Repo?
  cd existing_git_repo
  git remote add origin git@github.com:cssrain/Sharp-jQuery-Source.git
  git push -u origin master
      
Importing a Subversion Repo?
  Click here
      
When you're done:
  Continue